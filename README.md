# README

Gathers a list of all courses and imports that information into a database for a Django application.
Uses Python to fetch course data and Java to create the database.

## Running the app

From the project root, run:

```bash
bash bin/run.sh
```
